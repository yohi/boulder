# Tasks
* [ ] REINIT-1: `~/.config/boulder/` ディレクトリ構造を作成する (Scope: `boulder-reinit`)
* [ ] REINIT-2: 既存の `scripts/boulder-doctor.ts` を移動する (Scope: `boulder-reinit`)
* [ ] REINIT-3: 既存の `.cursor/rules/*.mdc` を移動する (Scope: `boulder-reinit`)
* [ ] REINIT-4: `package.json` を作成し依存関係を定義する (Scope: `boulder-reinit`)
* [ ] REINIT-5: CLI エントリーポイントを作成する (Scope: `boulder-reinit`)
* [ ] REINIT-6: `boulder-init.ts` を実装する (Scope: `boulder-reinit`)
* [ ] REINIT-7: シンボリックリンク作成処理を実装する (Scope: `boulder-reinit`)
* [ ] REINIT-8: 既存ディレクトリの確認・上書き確認ロジックを実装する (Scope: `boulder-reinit`)
* [ ] REINIT-9: `--force` フラグを実装する (Scope: `boulder-reinit`)
* [ ] REINIT-10: 初期化後の `boulder doctor` 自動実行を実装する (Scope: `boulder-reinit`)
* [ ] REINIT-11: `.devcontainer/` ディレクトリを作成する (Scope: `boulder-reinit`)
* [ ] REINIT-12: `devcontainer.json` を作成する (Scope: `boulder-reinit`)
* [ ] REINIT-13: `postCreateCommand` を設定する (Scope: `boulder-reinit`)
* [ ] REINIT-14: `@biomejs/biome` を devDependencies に追加する (Scope: `boulder-reinit`)
* [ ] REINIT-15: `biome.json` を作成する (Scope: `boulder-reinit`)
* [ ] REINIT-16: npm スクリプトを追加する (Scope: `boulder-reinit`)
* [ ] REINIT-17: Biome 存在確認チェックを追加する (Scope: `boulder-reinit`)
* [ ] REINIT-18: エラーメッセージとリカバリ手順を追加する (Scope: `boulder-reinit`)
* [ ] REINIT-19: シンボリックリンク状態確認を追加する (Scope: `boulder-reinit`)
* [ ] REINIT-20: Ralph Loop 検証順序を更新する (Scope: `boulder-reinit`)
* [ ] REINIT-21: Biome コマンドの説明を追加する (Scope: `boulder-reinit`)
* [ ] REINIT-22: `boulder init` でシンボリックリンクを確認する (Scope: `boulder-reinit`)
* [ ] REINIT-23: `devcontainer up` でコンテナを起動する (Scope: `boulder-reinit`)
* [ ] REINIT-24: `boulder doctor` が全チェックをパスすることを確認する (Scope: `boulder-reinit`)
* [ ] REINIT-25: `bun run check` が正常に動作することを確認する (Scope: `boulder-reinit`)
* [ ] REINIT-26: `bun test` が正常に動作することを確認する (Scope: `boulder-reinit`)
* [ ] REINIT-27: `README.md` にインストール手順を記述する (Scope: `boulder-reinit`)
* [ ] REINIT-28: グローバル配置とプロジェクト適用手順を記述する (Scope: `boulder-reinit`)
* [ ] REINIT-29: コマンドの使い方を記述する (Scope: `boulder-reinit`)
* [ ] REINIT-30: 環境モデルを記述する (Scope: `boulder-reinit`)
* [ ] REINIT-31: 前提条件を記述する (Scope: `boulder-reinit`)
